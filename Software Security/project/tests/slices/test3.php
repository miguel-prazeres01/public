<?php

    $a = b('username');
    while ($e == "") {
        $b=$c;
        $c=$d;
        $d=$a;
    }
    $g = h($b);

    // tip: sources, sanitizers and sinks can appear inside loops

?>